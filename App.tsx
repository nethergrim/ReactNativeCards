import React from 'react';
import {
  SafeAreaView,
} from 'react-native';
import { MainNavigationStack } from '@navigation';

export const App = () => {
  return (
    <MainNavigationStack />
  );
};
